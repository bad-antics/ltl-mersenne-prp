// ltl-mersenne-prp/main.ltl
//
// Lucas-Lehmer and Fermat PRP tests for Mersenne primes.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-mersenne-prp v0.1.0")
    io::println("Lucas-Lehmer and Fermat PRP tests for Mersenne primes.")
    0
}
