// examples/demo.ltl — runnable demo for ltl_mersenne_prp

import std::io
use ltl_mersenne_prp as lib

fn main() {
    io::println("ltl_mersenne_prp demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}